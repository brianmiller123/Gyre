[package]
name = "agent-snapcompact"
description = "图像化上下文压缩：本地 PNG 帧渲染（bitmap 字形），替换昂贵 summarize"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
ab_glyph = "0.2"
image = { workspace = true }
thiserror = { workspace = true }
