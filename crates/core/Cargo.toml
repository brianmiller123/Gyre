[package]
name        = "agent-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Agent core contracts: shared types, traits, and error types (zero business deps)"

[dependencies]
agent-iso   = { workspace = true }
serde       = { workspace = true }
serde_json  = { workspace = true }
thiserror   = { workspace = true }
anyhow      = { workspace = true }
async-trait = { workspace = true }
futures     = { workspace = true }
tracing     = { workspace = true }
tokio       = { workspace = true }
dirs        = { workspace = true }
regex       = { workspace = true }
sha2        = { workspace = true }
base64      = { workspace = true }
hmac        = { workspace = true }
rand        = { workspace = true }

[dev-dependencies]
reqwest    = { workspace = true }
# 基准门：tiktoken 计数基准（benches/tokenize.rs）。真实实现位于 agent-context
# （crates/context/src/token.rs）；core→context 的 dev 依赖边为 Cargo 允许的
# dev-dependency 环（context 正常依赖 core，dev 边不参与常规依赖图）。
tempfile    = { workspace = true }
agent-context = { workspace = true }
criterion = { workspace = true }

[lints]
workspace = true

[[bench]]
name = "tokenize"
harness = false
